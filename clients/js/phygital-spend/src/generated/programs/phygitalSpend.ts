/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfPlanAndSendFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getExecuteSpendInstructionAsync,
  parseExecuteSpendInstruction,
  type ExecuteSpendAsyncInput,
  type ParsedExecuteSpendInstruction,
} from "../instructions/index.js";
import { findSpendAuthorityPda } from "../pdas/index.js";

export const PHYGITAL_SPEND_PROGRAM_ADDRESS =
  "Gkst75NEFq6ojS9s9MCKtKcmEbGVL3SkcFxm3eVysxRF" as Address<"Gkst75NEFq6ojS9s9MCKtKcmEbGVL3SkcFxm3eVysxRF">;

export enum PhygitalSpendInstruction {
  ExecuteSpend,
}

export function identifyPhygitalSpendInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): PhygitalSpendInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([97, 195, 2, 242, 205, 203, 109, 210]),
      ),
      0,
    )
  ) {
    return PhygitalSpendInstruction.ExecuteSpend;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "phygitalSpend" },
  );
}

export type ParsedPhygitalSpendInstruction<
  TProgram extends string = "Gkst75NEFq6ojS9s9MCKtKcmEbGVL3SkcFxm3eVysxRF",
> = {
  instructionType: PhygitalSpendInstruction.ExecuteSpend;
} & ParsedExecuteSpendInstruction<TProgram>;

export function parsePhygitalSpendInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedPhygitalSpendInstruction<TProgram> {
  const instructionType = identifyPhygitalSpendInstruction(instruction);
  switch (instructionType) {
    case PhygitalSpendInstruction.ExecuteSpend: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: PhygitalSpendInstruction.ExecuteSpend,
        ...parseExecuteSpendInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "phygitalSpend",
        },
      );
  }
}

export type PhygitalSpendPlugin = {
  instructions: PhygitalSpendPluginInstructions;
  pdas: PhygitalSpendPluginPdas;
  identifyInstruction: typeof identifyPhygitalSpendInstruction;
  parseInstruction: typeof parsePhygitalSpendInstruction;
};

export type PhygitalSpendPluginInstructions = {
  executeSpend: (
    input: ExecuteSpendAsyncInput,
  ) => ReturnType<typeof getExecuteSpendInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type PhygitalSpendPluginPdas = {
  spendAuthority: typeof findSpendAuthorityPda;
};

export type PhygitalSpendPluginRequirements = ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function phygitalSpendProgram() {
  return <T extends PhygitalSpendPluginRequirements>(
    client: T,
  ): ExtendedClient<T, { phygitalSpend: PhygitalSpendPlugin }> => {
    return extendClient(client, {
      phygitalSpend: <PhygitalSpendPlugin>{
        instructions: {
          executeSpend: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getExecuteSpendInstructionAsync(input),
            ),
        },
        pdas: { spendAuthority: findSpendAuthorityPda },
        identifyInstruction: identifyPhygitalSpendInstruction,
        parseInstruction: parsePhygitalSpendInstruction,
      },
    });
  };
}
