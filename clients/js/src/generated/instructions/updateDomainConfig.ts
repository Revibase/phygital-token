/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PHYGITAL_TOKEN_PROGRAM_ADDRESS } from "../programs/index.js";

export const UPDATE_DOMAIN_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([226, 144, 2, 120, 193, 130, 251, 31]);

export function getUpdateDomainConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_DOMAIN_CONFIG_DISCRIMINATOR,
  );
}

export type UpdateDomainConfigInstruction<
  TProgram extends string = typeof PHYGITAL_TOKEN_PROGRAM_ADDRESS,
  TAccountDomainConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDomainConfig extends string
        ? WritableAccount<TAccountDomainConfig>
        : TAccountDomainConfig,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateDomainConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Option<Address>;
  newOrigins: Option<Array<string>>;
};

export type UpdateDomainConfigInstructionDataArgs = {
  newAuthority: OptionOrNullable<Address>;
  newOrigins: OptionOrNullable<Array<string>>;
};

export function getUpdateDomainConfigInstructionDataEncoder(): Encoder<UpdateDomainConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAuthority", getOptionEncoder(getAddressEncoder())],
      [
        "newOrigins",
        getOptionEncoder(
          getArrayEncoder(
            addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
          ),
        ),
      ],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_DOMAIN_CONFIG_DISCRIMINATOR,
    }),
  );
}

export function getUpdateDomainConfigInstructionDataDecoder(): Decoder<UpdateDomainConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAuthority", getOptionDecoder(getAddressDecoder())],
    [
      "newOrigins",
      getOptionDecoder(
        getArrayDecoder(
          addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
        ),
      ),
    ],
  ]);
}

export function getUpdateDomainConfigInstructionDataCodec(): Codec<
  UpdateDomainConfigInstructionDataArgs,
  UpdateDomainConfigInstructionData
> {
  return combineCodec(
    getUpdateDomainConfigInstructionDataEncoder(),
    getUpdateDomainConfigInstructionDataDecoder(),
  );
}

export type UpdateDomainConfigInput<
  TAccountDomainConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  domainConfig: Address<TAccountDomainConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: UpdateDomainConfigInstructionDataArgs["newAuthority"];
  newOrigins: UpdateDomainConfigInstructionDataArgs["newOrigins"];
};

export function getUpdateDomainConfigInstruction<
  TAccountDomainConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PHYGITAL_TOKEN_PROGRAM_ADDRESS,
>(
  input: UpdateDomainConfigInput<
    TAccountDomainConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateDomainConfigInstruction<
  TProgramAddress,
  TAccountDomainConfig,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_TOKEN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    domainConfig: { value: input.domainConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("domainConfig", accounts.domainConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUpdateDomainConfigInstructionDataEncoder().encode(
      args as UpdateDomainConfigInstructionDataArgs,
    ),
    programAddress,
  } as UpdateDomainConfigInstruction<
    TProgramAddress,
    TAccountDomainConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedUpdateDomainConfigInstruction<
  TProgram extends string = typeof PHYGITAL_TOKEN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    domainConfig: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: UpdateDomainConfigInstructionData;
};

export function parseUpdateDomainConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateDomainConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      domainConfig: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdateDomainConfigInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
