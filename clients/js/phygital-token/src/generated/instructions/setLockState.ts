/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { PHYGITAL_TOKEN_PROGRAM_ADDRESS } from "../programs/index.js";

export const SET_LOCK_STATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  16, 40, 80, 140, 163, 156, 68, 120,
]);

export function getSetLockStateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_LOCK_STATE_DISCRIMINATOR,
  );
}

export type SetLockStateInstruction<
  TProgram extends string = typeof PHYGITAL_TOKEN_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountPhygitalToken extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPhygitalToken extends string
        ? WritableAccount<TAccountPhygitalToken>
        : TAccountPhygitalToken,
      ...TRemainingAccounts,
    ]
  >;

export type SetLockStateInstructionData = {
  discriminator: ReadonlyUint8Array;
  isLocked: boolean;
};

export type SetLockStateInstructionDataArgs = { isLocked: boolean };

export function getSetLockStateInstructionDataEncoder(): FixedSizeEncoder<SetLockStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["isLocked", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_LOCK_STATE_DISCRIMINATOR }),
  );
}

export function getSetLockStateInstructionDataDecoder(): FixedSizeDecoder<SetLockStateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["isLocked", getBooleanDecoder()],
  ]);
}

export function getSetLockStateInstructionDataCodec(): FixedSizeCodec<
  SetLockStateInstructionDataArgs,
  SetLockStateInstructionData
> {
  return combineCodec(
    getSetLockStateInstructionDataEncoder(),
    getSetLockStateInstructionDataDecoder(),
  );
}

export type SetLockStateInput<
  TAccountOwner extends string = string,
  TAccountPhygitalToken extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  phygitalToken: Address<TAccountPhygitalToken>;
  isLocked: SetLockStateInstructionDataArgs["isLocked"];
};

export function getSetLockStateInstruction<
  TAccountOwner extends string,
  TAccountPhygitalToken extends string,
  TProgramAddress extends Address = typeof PHYGITAL_TOKEN_PROGRAM_ADDRESS,
>(
  input: SetLockStateInput<TAccountOwner, TAccountPhygitalToken>,
  config?: { programAddress?: TProgramAddress },
): SetLockStateInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountPhygitalToken
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_TOKEN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    phygitalToken: { value: input.phygitalToken ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("phygitalToken", accounts.phygitalToken),
    ],
    data: getSetLockStateInstructionDataEncoder().encode(
      args as SetLockStateInstructionDataArgs,
    ),
    programAddress,
  } as SetLockStateInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountPhygitalToken
  >);
}

export type ParsedSetLockStateInstruction<
  TProgram extends string = typeof PHYGITAL_TOKEN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    phygitalToken: TAccountMetas[1];
  };
  data: SetLockStateInstructionData;
};

export function parseSetLockStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetLockStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), phygitalToken: getNextAccount() },
    data: getSetLockStateInstructionDataDecoder().decode(instruction.data),
  };
}
